#include "holberton.h"

/**
 *cap_string-capital
 *Return:char
 *betty comment????????
 */
char *cap_string(char *)
{
	char[] letters = {' ', '\n', ',', ';', '.', '!', '?', '"', '(', ')', '{', '}'};
	int i;

	for (i = 0 ; i < 
}
